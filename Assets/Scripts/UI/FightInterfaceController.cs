using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FightInterfaceController : MonoBehaviour
{
    public GameObject Time;
    // Start is called before the first frame update
    void Start()
    {
        Time.SetActive(false);
    }
}
